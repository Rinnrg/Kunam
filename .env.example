# Database Configuration
# =====================
# PENTING: Untuk optimasi performa, tambahkan parameter connection pooling!
# Format: ?connection_limit=10&pool_timeout=20&connect_timeout=10

DATABASE_URL="postgresql://user:password@host:port/database?connection_limit=10&pool_timeout=20&connect_timeout=10"
DIRECT_URL="postgresql://user:password@host:port/database"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your_nextauth_secret_key_here"

# Generate a secret with: openssl rand -base64 32

# Cloudinary (Required for file uploads in production)
# Sign up for free at: https://cloudinary.com
CLOUDINARY_CLOUD_NAME="your_cloud_name"
CLOUDINARY_API_KEY="your_api_key"
CLOUDINARY_API_SECRET="your_api_secret"

# Performance Tips
# ================
# 1. Connection limit: Sesuaikan dengan plan database Anda (5-20)
# 2. Pool timeout: Waktu tunggu untuk mendapat koneksi (default: 20 detik)
# 3. Connect timeout: Timeout untuk koneksi baru (default: 10 detik)
# 4. Untuk production, gunakan DIRECT_URL untuk migrations

